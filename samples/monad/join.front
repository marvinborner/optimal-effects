-- x >>= id
let mjoin x = do (
	a <- x
	a
)

do (
	-- loud echo
	_ <- mjoin (do (
		x <- readInt <>
		return (writeInt (mul x x !))
	))
	
	-- sum two numbers
	mjoin (do (
		x <- readInt <>
		y <- readInt <>
		return (writeInt (add x y !))
	))
)
