let fib n = (
  if (isEqual 0 n)
    then 0
    else if (isEqual 1 n)
      then 1
      -- else (join (add (fib (sub n 1))) (fib (sub n 2)))
      else (add (fib (sub n 1)) (fib (sub n 2)))
)

writeInt (fib 4)
